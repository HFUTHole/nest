export enum NotifyEventType {
  comment = 'comment',
  reply = 'reply',
  like = 'like',
  collect = 'collect',
}

export enum InteractionNotifyTargetType {
  comment = 'comment',
  reply = ' reply',
  post = 'post',
  usedGoods = 'usedGoods',
}

export enum NotifyStatus {
  unread,

  read,
}
