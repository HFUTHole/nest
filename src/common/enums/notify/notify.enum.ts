export enum NotifyEventType {
  comment = 'comment',
  reply = 'reply',
  like = 'like',
}

export enum InteractionNotifyTargetType {
  comment = 'comment',
  reply = ' reply',
  post = 'post',
}

export enum NotifyStatus {
  unread,

  read,
}
